package com.lakshmi.springboot.ToDoApp.Service;


/*Deprecated AuthenticationService after introducing Spring Security in the project*/

/*
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

@Service
public class AuthenticationService
{
    private Logger logger = LoggerFactory.getLogger((getClass()));


    public boolean authenticateUser (String userName, String password)
    {
        logger.debug("Username is:" + userName);
        logger.debug("Password is:" + password);

        if (userName.equals("Nisha") && password.equals("ilikethis"))
        {

            logger.debug("Username matches with password");
            return true;
        }
        else
        {
            logger.debug("Username and password do not match");
            return false;
        }
    }
}
*/

